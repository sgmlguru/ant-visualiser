# Ant Functionality to Visualise

## Macros

Macros are usually defined in imported, separate files. The approach should be to 

1. List them all - repreentation in taskdef node and similar
2. Use the list when matching used macros


## Misc

* `copy`
* `mkdir`
* `fileset`
	* (attributes?)
	* `include`
	* `exclude`
* `condition`
	* `isset`
	* ...
